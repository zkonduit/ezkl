name: Bump Version

on:
  push:
    branches: ["main"]

jobs:
  bump-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Cargo.toml version to match github tag for docs
        shell: bash
        env:
          RELEASE_TAG: ${{ steps.tag_version.outputs.new_tag }}
        run: |
          cp docs/python/src/conf.py docs/python/src/conf.py.orig
          sed "s/0\\.0\\.0/${RELEASE_TAG//v}/" docs/python/src/conf.py.orig >docs/python/src/conf.py
          cp docs/python/requirements-docs.txt docs/python/requirements-docs.txt.orig
          sed "s/0\\.0\\.0/${RELEASE_TAG//v}/" docs/python/requirements-docs.txt.orig >docs/python/requirements-docs.txt

      - name: Commit files
<<<<<<< HEAD
        env:
          RELEASE_TAG: ${{ steps.tag_version.outputs.new_tag }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git tag -d $RELEASE_TAG
          git tag $RELEASE_TAG
          git commit -a -m "ci: update version string in docs"
=======
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git tag -d $steps.tag_version.outputs.new_tag
          git tag $steps.tag_version.outputs.new_tag
          git commit -a -m "ci: update documentation versions"
>>>>>>> 07e2fe93382fd1b98521283347e9df32eb0bc577

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          force: true
          tags: true
